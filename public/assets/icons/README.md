# Workspace 离线图标

本目录保存工作台“快速入口”和“全部应用”使用的本地图标，避免内网环境依赖远程图片 URL。

- `quick-entry/`：快速入口使用的 96×96 PNG。
- `app-catalog/`：全部应用卡片使用的 96×96 PNG。

图标来自 Icons8 Fluency 图标集。发布或对外分发前，应根据实际授权方式确认是否需要保留
Icons8 署名或商业许可。代码中统一通过 `/assets/icons/...` 引用，不应重新引入远程 URL。
